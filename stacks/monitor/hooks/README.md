# M0 placeholder

This directory is reserved by the stack contract. Renderable templates, init
assets, or hooks are added in the stack's implementation milestone and tested
with golden fixtures before deployment is enabled.
