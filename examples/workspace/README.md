# Synthetic mksrv workspace

This directory contains fake, documentation-only data. It is safe for CI and is
used by `mksrv validate examples/workspace`. No value identifies a real account,
host, person, or domain.

A real workspace is private and must live outside the public engine repository.
Copy the shape, not the placeholder identifiers.
